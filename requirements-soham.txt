# Add these to the project's shared requirements.txt (or keep separate
# and merge with Kshitij's mcp/requirements.txt before submission).

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # required for FastAPI file/form uploads
httpx>=0.27               # WhatsApp Graph API calls
openai-whisper             # ASR backend for asr/transcribe.py
openai>=1.30               # OpenAI-compatible SDK, used to call Groq